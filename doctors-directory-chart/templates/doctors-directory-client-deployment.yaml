apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.stage-prefix }}-doctors-directory-client-deployment
  namespace: {{ .Values.namespace }}-doctors-directory
  labels:
    app: {{ .Values.stage-prefix }}-doctors-directory-client
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.stage-prefix }}-doctors-directory-client
  template:
    metadata:
      namespace: {{ .Values.namespace }}-doctors-directory
      labels:
        app: {{ .Values.stage-prefix }}-doctors-directory-client
    spec:
      containers:
        - name: {{ .Values.stage-prefix }}-doctors-directory-client
          image: tm63/doctors-directory-react-gql-client:host-env-update-commit
          ports:
            - containerPort: 3000
          env:
            - name: REACT_APP_SERVER_HOST
              valueFrom:
                configMapKeyRef:
                  name: doctors-directory-client-configmap
                  key: server_host

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.stage-prefix }}-doctors-directory-client-service
  namespace: {{ .Values.namespace }}-doctors-directory
spec:
  selector:
    app: {{ .Values.stage-prefix }}-doctors-directory-client
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000