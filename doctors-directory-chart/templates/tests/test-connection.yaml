apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "doctors-directory-chart.fullname" . }}-test-connection"
  labels:
    {{- include "doctors-directory-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "doctors-directory-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
